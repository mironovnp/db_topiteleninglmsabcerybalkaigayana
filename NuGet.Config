<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Slow or flaky networks: default NuGet HTTP timeout is short; bump for restore. -->
  <config>
    <add key="httpTimeout" value="600000" />
  </config>
</configuration>
